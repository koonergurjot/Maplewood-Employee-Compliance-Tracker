name: Badges

on:
  push:
    branches: [work, main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Placeholder
        run: echo "Badge workflow placeholder"
